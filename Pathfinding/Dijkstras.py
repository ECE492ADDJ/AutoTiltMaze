#!/usr/bin/python

class Dijkstras:
	'Find the shortest path between two nodes retrieved from image'

	# Will need to decide whether we will store start/end inside a node or just pass it
	def __init__(self, nodeList, startNode, endNode):
		self.unvistedNodes = nodeList
		print("Initialized")

	def testFunc(self):
		print("test2") 

